import * as actions from './actions/fetch-actions';
import reducer, * as selectors from './reducers';

export default {
  actions,
  reducer,
  selectors
};

export {
  actions,
  reducer,
  selectors
};
